import 'package:flutter/material.dart';

class MasVendidosPage extends StatelessWidget {
  const MasVendidosPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mas Vendidos'),
    );
  }
}

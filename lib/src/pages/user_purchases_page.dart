import 'package:flutter/material.dart';

class UserPurchases extends StatelessWidget {
  const UserPurchases({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Compras'),
    );
  }
}
